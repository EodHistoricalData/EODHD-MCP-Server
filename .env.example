# EODHD MCP Server Configuration

# =============================================================================
# Required: EODHD API Key
# =============================================================================
# Your EODHD API key for accessing financial data
# Get yours at: https://eodhd.com/register
EODHD_API_KEY=your_api_key_here

# =============================================================================
# Server Configuration
# =============================================================================
# Host to bind the server to
MCP_HOST=127.0.0.1

# Port to bind the server to
MCP_PORT=8000

# Path for legacy HTTP endpoint (only used in non-OAuth mode)
MCP_PATH=/mcp

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# OAuth 2.1 Configuration
# =============================================================================
# Enable OAuth 2.1 mode
# When enabled, server runs with /mcp (legacy), /mcp-oauth (OAuth), and auth server
OAUTH_ENABLED=false

# Canonical server URL (used in OAuth metadata)
# This should be your public server URL
MCP_SERVER_URL=http://localhost:8000

# OAuth Authorization Server URL (issuer)
# Since we're running our own OAuth server, this should match MCP_SERVER_URL
OAUTH_ISSUER=http://localhost:8000

# =============================================================================
# JWT Configuration
# =============================================================================
# Secret key for signing JWT access tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# IMPORTANT: Change this in production!
JWT_SECRET=change_this_secret_in_production

# JWT algorithm (default: HS256)
# JWT_ALGORITHM=HS256

# Access token expiration time in seconds (default: 3600 = 1 hour)
ACCESS_TOKEN_EXPIRES=3600

# Authorization code expiration time in seconds (default: 600 = 10 minutes)
AUTH_CODE_EXPIRES=600

# =============================================================================
# Session Configuration
# =============================================================================
# Secret key for session middleware
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# IMPORTANT: Change this in production!
SESSION_SECRET=change_this_secret_in_production

# =============================================================================
# External OAuth Configuration (Optional)
# =============================================================================
# If you want to use an external OAuth server instead of the built-in one,
# configure these endpoints:

# Token introspection endpoint (RFC 7662)
# OAUTH_INTROSPECTION_URL=https://oauth.example.com/introspect

# JWKS endpoint for JWT validation
# OAUTH_JWKS_URL=https://oauth.example.com/.well-known/jwks.json

# =============================================================================
# Development/Testing
# =============================================================================
# Demo user credentials (built-in):
#   Username: demo
#   Password: demo123
#
# These credentials are hardcoded for testing purposes.
# In production, you should implement proper user management.
