---
extends: default

ignore:
  - .venv/
  - node_modules/

rules:
  line-length: disable
  indentation:
    spaces: 2
    indent-sequences: whatever
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  document-start: disable
